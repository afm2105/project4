<h1> Edit president>

  <%= render 'form' %>

  <%= link_to 'Back', presidents_path %>

<!-- <h1>Editing president</h1>

<%= form_for :president, url: president_path(@president), method: :patch do |f| %>

  <% if @president.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@president.errors.count, "error") %> prohibited
        this president from being saved:
      </h2>
      <ul>
        <% @president.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :text %>
  </p>

  <p>
    <%= f.label :facts %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', presidents_path %> -->
